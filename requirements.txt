# Web app dependencies ONLY -- this is what Railway auto-detects and
# installs to run webapp/main.py. Deliberately does NOT include the
# pipeline's heavy GRIB2/geospatial stack (cfgrib, eccodes, xarray,
# pyproj, etc.) -- the web app only ever reads already-generated
# GeoJSON files from disk; it never touches NBM data directly. See
# requirements-pipeline.txt for everything the data-generation side
# needs (installed separately, only in GitHub Actions).
fastapi
uvicorn[standard]
